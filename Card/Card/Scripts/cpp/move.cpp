/*#include "DxLib.h"

//WinMain関数(普通のプログラムで言うところのMainかな？)
int WINAPI WinMain(HINSTANCE hInstance, HINSTANCE hPrevInstance, LPSTR lpCmdLine, int nCmdShow){
	ChangeWindowMode(true);//ウィンドウモードに設定
	if (DxLib_Init() == -1)return -1;//DXlibの初期化
	WaitKey();//キー入力を待機する
	return 0;
}*/